var pairsByIndexRaw = [[0, 3], [1, 2], [2, 1], null, [1], false, "whoops"];

// function isPair(element) {
//   return element.length === 2 || element === undefined;
// }

// isPair(pairsByIndexRaw[4]);

//Array.isArray(pairsByIndexRaw[4]);

function isAnArray (index){
    Array.isArray(pairsByIndexRaw[index]);
}

isAnArray(2);
// var pairsByIndex = pairsByIndexRaw.filter(
    









// 1. find if 1 element is array of 2 items 